version: '3.9'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - 3000:3000

  frontend:
    build:
      context: ./backend
      dockerfile: Dockerfile
  bff:
    build:
      context: ./backend
      dockerfile: Dockerfile
  redis:
    image: redis
  rabbitmq:
    image: rqbbitmq
  mariadb:
    image: mariadb
